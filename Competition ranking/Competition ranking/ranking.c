#include<stdio.h>

struct Connect
{
	int i;
	struct Connect* p;
};

int main()
{
	char ranking[6] = { 0 }; //代表a,b,c,d,e字符，将名次存入

	int loop[6] = { 1,2,3,4,5 };

	int i = 0;
	for (ranking[1] = 1; ranking[1] <= 5; ranking[1]++)
	{

	}

	return 0;
}